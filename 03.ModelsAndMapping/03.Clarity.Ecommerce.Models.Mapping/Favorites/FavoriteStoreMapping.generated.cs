// <autogenerated>
// <copyright file="Mapping.Favorites.FavoriteStore.cs" company="clarity-ventures.com">
// Copyright (c) 2016-2023 clarity-ventures.com. All rights reserved.
// </copyright>
// <summary>Implements the Favorites section of the Mapping class</summary>
// <remarks>This file was auto-generated by Mapping.tt, changes to this
// file will be overwritten automatically when the T4 template is run again</remarks>
// </autogenerated>
// ReSharper disable CyclomaticComplexity, FunctionComplexityOverflow, InvokeAsExtensionMethod, MergeCastWithTypeCheck
// ReSharper disable MissingLinebreak, RedundantDelegateInvoke, RedundantUsingDirective
#pragma warning disable CS0618 // Ignore Obsolete warnings
#nullable enable
namespace Clarity.Ecommerce.Mapper
{
    using System;
    using System.Collections.Generic;
    using System.Data.Entity;
    using System.Linq;
    using System.Linq.Expressions;
    using DataModel;
    using Interfaces.DataModel;
    using Interfaces.Models;
    using LinqKit;
    using MoreLinq;
    using Utilities;

    public static partial class ModelMapperForFavoriteStore
    {
        public sealed class AnonFavoriteStore : FavoriteStore
        {
            public Contact? MasterContact { get; set; }
            // public new User? Master { get; set; }
        }

        public static readonly Func<FavoriteStore?, string?, IFavoriteStoreModel?> MapFavoriteStoreModelFromEntityFull = CreateFavoriteStoreModelFromEntityFull;

        public static readonly Func<FavoriteStore?, string?, IFavoriteStoreModel?> MapFavoriteStoreModelFromEntityLite = CreateFavoriteStoreModelFromEntityLite;

        public static readonly Func<FavoriteStore?, string?, IFavoriteStoreModel?> MapFavoriteStoreModelFromEntityList = CreateFavoriteStoreModelFromEntityList;

        public static Func<IFavoriteStore, IFavoriteStoreModel, string?, IFavoriteStoreModel>? CreateFavoriteStoreModelFromEntityHooksFull { get; set; }

        public static Func<IFavoriteStore, IFavoriteStoreModel, string?, IFavoriteStoreModel>? CreateFavoriteStoreModelFromEntityHooksLite { get; set; }

        public static Func<IFavoriteStore, IFavoriteStoreModel, string?, IFavoriteStoreModel>? CreateFavoriteStoreModelFromEntityHooksList { get; set; }

        public static Expression<Func<FavoriteStore, AnonFavoriteStore>>? PreBuiltFavoriteStoreSQLSelectorFull { get; set; }

        public static Expression<Func<FavoriteStore, AnonFavoriteStore>>? PreBuiltFavoriteStoreSQLSelectorLite { get; set; }

        public static Expression<Func<FavoriteStore, AnonFavoriteStore>>? PreBuiltFavoriteStoreSQLSelectorList { get; set; }

        /// <summary>An <see cref="IFavoriteStoreModel"/> extension method that creates a(n) <see cref="FavoriteStore"/> entity.</summary>
        /// <param name="model">            The model to act on.</param>
        /// <param name="timestamp">        The timestamp Date/Time.</param>
        /// <param name="contextProfileName">Name of the context profile.</param>
        /// <returns>The new <see cref="FavoriteStore"/> entity.</returns>
        public static IFavoriteStore CreateFavoriteStoreEntity(
            this IFavoriteStoreModel model,
            DateTime timestamp,
            string? contextProfileName)
        {
            // Create the object and Map the Inherited Properties
            return Contract.RequiresNotNull(model)
                .CreateEntityFromModelBase<IFavoriteStoreModel, FavoriteStore>(timestamp, contextProfileName)
                // Use the Update method to map non-inherited properties
                .UpdateFavoriteStoreFromModel(model, timestamp, null);
        }

        /// <summary>An <see cref="IFavoriteStoreModel"/> extension method that updates a(n) <see cref="FavoriteStore"/> entity.</summary>
        /// <param name="entity">         The entity to act on.</param>
        /// <param name="model">          The model to read from.</param>
        /// <param name="timestamp">      The timestamp Date/Time.</param>
        /// <param name="updateTimestamp">The update timestamp Date/Time.</param>
        /// <returns>The updated <see cref="FavoriteStore"/> entity.</returns>
        public static IFavoriteStore UpdateFavoriteStoreFromModel(
            this IFavoriteStore entity,
            IFavoriteStoreModel model,
            DateTime timestamp,
            DateTime? updateTimestamp)
        {
            // Map the Inherited Properties
            entity = Contract.RequiresNotNull(entity)
                .MapIAmARelationshipTableBaseModelPropertiesToEntity(Contract.RequiresNotNull(model));
            // FavoriteStore's Related Objects
            // Finally, update the timestamp
            entity.UpdatedDate = updateTimestamp == null || updateTimestamp == DateTime.MinValue ? null : updateTimestamp;
            // Return
            return entity;
        }

        public static void GenFavoriteStoreSQLSelectorFull()
        {
            PreBuiltFavoriteStoreSQLSelectorFull = x => x == null ? null! : new AnonFavoriteStore
            {
                MasterID = x.MasterID,
                Master = ModelMapperForUser.PreBuiltUserSQLSelectorList.Expand().Compile().Invoke(x.Master!),
                SlaveID = x.SlaveID,
                Slave = ModelMapperForStore.PreBuiltStoreSQLSelectorList.Expand().Compile().Invoke(x.Slave!),
                ID = x.ID,
                CustomKey = x.CustomKey,
                CreatedDate = x.CreatedDate,
                UpdatedDate = x.UpdatedDate,
                Active = x.Active,
                Hash = x.Hash,
                JsonAttributes = x.JsonAttributes,
            };
        }

        public static void GenFavoriteStoreSQLSelectorLite()
        {
            PreBuiltFavoriteStoreSQLSelectorLite = x => x == null ? null! : new AnonFavoriteStore
            {
                MasterID = x.MasterID,
                Master = ModelMapperForUser.PreBuiltUserSQLSelectorList.Expand().Compile().Invoke(x.Master!),
                SlaveID = x.SlaveID,
                Slave = ModelMapperForStore.PreBuiltStoreSQLSelectorList.Expand().Compile().Invoke(x.Slave!),
                ID = x.ID,
                CustomKey = x.CustomKey,
                CreatedDate = x.CreatedDate,
                UpdatedDate = x.UpdatedDate,
                Active = x.Active,
                Hash = x.Hash,
                JsonAttributes = x.JsonAttributes,
            };
        }

        public static void GenFavoriteStoreSQLSelectorList()
        {
            PreBuiltFavoriteStoreSQLSelectorList = x => x == null ? null! : new AnonFavoriteStore
            {
                MasterID = x.MasterID,
                Master = ModelMapperForUser.PreBuiltUserSQLSelectorList.Expand().Compile().Invoke(x.Master!), // For Flattening Properties (List)
                SlaveID = x.SlaveID,
                Slave = ModelMapperForStore.PreBuiltStoreSQLSelectorList.Expand().Compile().Invoke(x.Slave!), // For Flattening Properties (List)
                ID = x.ID,
                CustomKey = x.CustomKey,
                CreatedDate = x.CreatedDate,
                Active = x.Active,
                Hash = x.Hash,
                JsonAttributes = x.JsonAttributes,
            };
        }

        public static IEnumerable<IFavoriteStoreModel> SelectFullFavoriteStoreAndMapToFavoriteStoreModel(
            this IQueryable<FavoriteStore> dbSetWithFilters,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltFavoriteStoreSQLSelectorFull == null);
            return dbSetWithFilters
                .AsExpandable()
                .Select(PreBuiltFavoriteStoreSQLSelectorFull!.Compile())
                .ToList()
                .Select(x => CreateFavoriteStoreModelFromEntityFull(x, contextProfileName))!;
        }

        public static IEnumerable<IFavoriteStoreModel> SelectLiteFavoriteStoreAndMapToFavoriteStoreModel(
            this IQueryable<FavoriteStore> dbSetWithFilters,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltFavoriteStoreSQLSelectorLite == null);
            return dbSetWithFilters
                .AsExpandable()
                .Select(PreBuiltFavoriteStoreSQLSelectorLite!.Compile())
                .ToList()
                .Select(x => CreateFavoriteStoreModelFromEntityLite(x, contextProfileName))!;
        }

        public static IEnumerable<IFavoriteStoreModel> SelectListFavoriteStoreAndMapToFavoriteStoreModel(
            this IQueryable<FavoriteStore> dbSetWithFilters,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltFavoriteStoreSQLSelectorList == null);
            return dbSetWithFilters
                .AsExpandable()
                .Select(PreBuiltFavoriteStoreSQLSelectorList!.Compile())
                .ToList()
                .Select(x => CreateFavoriteStoreModelFromEntityList(x, contextProfileName))!;
        }

        public static IFavoriteStoreModel? SelectFirstFullFavoriteStoreAndMapToFavoriteStoreModel(
            this IQueryable<FavoriteStore> dbSetWithFilters,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltFavoriteStoreSQLSelectorFull == null);
            return dbSetWithFilters
                .AsExpandable()
                .OrderBy(x => x.ID)
                .Take(1)
                .Select(PreBuiltFavoriteStoreSQLSelectorFull!.Compile())
                .ToList()
                .Select(x => CreateFavoriteStoreModelFromEntityFull(x, contextProfileName))
                .FirstOrDefault();
        }

        public static IFavoriteStoreModel? SelectFirstListFavoriteStoreAndMapToFavoriteStoreModel(
            this IQueryable<FavoriteStore> dbSetWithFilters,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltFavoriteStoreSQLSelectorList == null);
            return dbSetWithFilters
                .AsExpandable()
                .OrderBy(x => x.ID)
                .Take(1)
                .Select(PreBuiltFavoriteStoreSQLSelectorList!.Compile())
                .ToList()
                .Select(x => CreateFavoriteStoreModelFromEntityList(x, contextProfileName))
                .FirstOrDefault();
        }

        public static IFavoriteStoreModel? SelectSingleFullFavoriteStoreAndMapToFavoriteStoreModel(
            this IQueryable<FavoriteStore> dbSetWithFilters,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltFavoriteStoreSQLSelectorFull == null);
            return dbSetWithFilters
                .AsExpandable()
                .OrderBy(x => x.ID)
                .Take(1)
                .Select(PreBuiltFavoriteStoreSQLSelectorFull!.Compile())
                .ToList()
                .Select(x => CreateFavoriteStoreModelFromEntityFull(x, contextProfileName))
                .SingleOrDefault();
        }

        public static IFavoriteStoreModel? SelectSingleLiteFavoriteStoreAndMapToFavoriteStoreModel(
            this IQueryable<FavoriteStore> dbSetWithFilters,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltFavoriteStoreSQLSelectorLite == null);
            return dbSetWithFilters
                .AsExpandable()
                .OrderBy(x => x.ID)
                .Take(1)
                .Select(PreBuiltFavoriteStoreSQLSelectorLite!.Compile())
                .ToList()
                .Select(x => CreateFavoriteStoreModelFromEntityLite(x, contextProfileName))
                .SingleOrDefault();
        }

        public static IFavoriteStoreModel? SelectSingleListFavoriteStoreAndMapToFavoriteStoreModel(
            this IQueryable<FavoriteStore> dbSetWithFilters,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltFavoriteStoreSQLSelectorList == null);
            return dbSetWithFilters
                .AsExpandable()
                .OrderBy(x => x.ID)
                .Take(1)
                .Select(PreBuiltFavoriteStoreSQLSelectorList!.Compile())
                .ToList()
                .Select(x => CreateFavoriteStoreModelFromEntityList(x, contextProfileName))
                .SingleOrDefault();
        }

        public static (IEnumerable<IFavoriteStoreModel> results, int totalPages, int totalCount) SelectFullFavoriteStoreAndMapToFavoriteStoreModel(
            this IQueryable<FavoriteStore> dbSetWithFilters,
            Paging? paging,
            Sort[]? sorts,
            Grouping[]? groupings,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltFavoriteStoreSQLSelectorFull == null);
            return (dbSetWithFilters
                    .AsExpandable()
                    .ApplySorting(sorts, groupings, contextProfileName)
                    .FilterByPaging(paging, out var totalPages, out var totalCount)
                    .Select(PreBuiltFavoriteStoreSQLSelectorFull!.Compile())
                    .ToList()
                    .Select(x => CreateFavoriteStoreModelFromEntityFull(x, contextProfileName)),
                totalPages,
                totalCount)!;
        }

        public static (IEnumerable<IFavoriteStoreModel> results, int totalPages, int totalCount) SelectLiteFavoriteStoreAndMapToFavoriteStoreModel(
            this IQueryable<FavoriteStore> dbSetWithFilters,
            Paging? paging,
            Sort[]? sorts,
            Grouping[]? groupings,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltFavoriteStoreSQLSelectorLite == null);
            return (dbSetWithFilters
                    .AsExpandable()
                    .ApplySorting(sorts, groupings, contextProfileName)
                    .FilterByPaging(paging, out var totalPages, out var totalCount)
                    .Select(PreBuiltFavoriteStoreSQLSelectorLite!.Compile())
                    .ToList()
                    .Select(x => CreateFavoriteStoreModelFromEntityLite(x, contextProfileName)),
                totalPages,
                totalCount)!;
        }

        public static (IEnumerable<IFavoriteStoreModel> results, int totalPages, int totalCount) SelectListFavoriteStoreAndMapToFavoriteStoreModel(
            this IQueryable<FavoriteStore> dbSetWithFilters,
            Paging? paging,
            Sort[]? sorts,
            Grouping[]? groupings,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltFavoriteStoreSQLSelectorList == null);
            return (dbSetWithFilters
                    .AsExpandable()
                    .ApplySorting(sorts, groupings, contextProfileName)
                    .FilterByPaging(paging, out var totalPages, out var totalCount)
                    .Select(PreBuiltFavoriteStoreSQLSelectorList!.Compile())
                    .ToList()
                    .Select(x => CreateFavoriteStoreModelFromEntityList(x, contextProfileName)),
                totalPages,
                totalCount)!;
        }

        public static IFavoriteStoreModel? CreateFavoriteStoreModelFromEntityFull(this IFavoriteStore? entity, string? contextProfileName)
        {
            return CreateFavoriteStoreModelFromEntity(entity, MappingMode.Full, contextProfileName);
        }

        public static IFavoriteStoreModel? CreateFavoriteStoreModelFromEntityLite(this IFavoriteStore? entity, string? contextProfileName)
        {
            return CreateFavoriteStoreModelFromEntity(entity, MappingMode.Lite, contextProfileName);
        }

        public static IFavoriteStoreModel? CreateFavoriteStoreModelFromEntityList(this IFavoriteStore? entity, string? contextProfileName)
        {
            return CreateFavoriteStoreModelFromEntity(entity, MappingMode.List, contextProfileName);
        }

        public static IFavoriteStoreModel? CreateFavoriteStoreModelFromEntity(
            this IFavoriteStore? entity,
            MappingMode mode,
            string? contextProfileName)
        {
            if (entity == null) { return null; }
            // Map the Inherited Properties
            // ReSharper disable once InvokeAsExtensionMethod
            var model = BaseModelMapper.MapBaseEntityPropertiesToModel(
                RegistryLoaderWrapper.GetInstance<IFavoriteStoreModel>(contextProfileName),
                Contract.RequiresNotNull(entity),
                mode,
                contextProfileName);
            // Map this level's Properties
            switch (mode)
            {
                case MappingMode.Full: //////////////////////////////////////////////////////////////////////////////////////////////////////////////
                {
                    // FavoriteStore's Properties
                    // FavoriteStore's Related Objects
                    // FavoriteStore's Associated Objects
                    // Additional Mappings
                    if (CreateFavoriteStoreModelFromEntityHooksFull != null) { model = CreateFavoriteStoreModelFromEntityHooksFull(entity, model, contextProfileName); }
                    goto case MappingMode.Lite;
                }
                case MappingMode.Lite: //////////////////////////////////////////////////////////////////////////////////////////////////////////////
                {
                    // FavoriteStore's Properties
                    // FavoriteStore's Related Objects (Not Mapped unless Forced, or a flattening property)
                    // FavoriteStore's Associated Objects (Not Mapped unless Forced)
                    // Additional Mappings
                    if (CreateFavoriteStoreModelFromEntityHooksLite != null) { model = CreateFavoriteStoreModelFromEntityHooksLite(entity, model, contextProfileName); }
                    goto case MappingMode.List;
                }
                case MappingMode.List: //////////////////////////////////////////////////////////////////////////////////////////////////////////////
                default:
                {
                    // IHaveJsonAttributesBase Properties (Forced)
                    model.SerializableAttributes = entity.JsonAttributes.DeserializeAttributesDictionary();
                    // FavoriteStore's Properties
                    // FavoriteStore's Related Objects (Not Mapped unless Forced, or a flattening property)
                    model.MasterID = entity.MasterID;
                    model.MasterKey = entity.Master?.CustomKey;
                    model.SlaveID = entity.SlaveID;
                    model.Slave = ModelMapperForStore.CreateStoreModelFromEntityLite(entity.Slave, contextProfileName);
                    model.SlaveKey = entity.Slave?.CustomKey;
                    model.SlaveName = entity.Slave?.Name;
                    // FavoriteStore's Associated Objects (Not Mapped unless Forced)
                    // Additional Mappings
                    if (CreateFavoriteStoreModelFromEntityHooksList != null) { model = CreateFavoriteStoreModelFromEntityHooksList(entity, model, contextProfileName); }
                    break;
                }
            } ///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
            // Finished!
            return model;
        }
    }
}
