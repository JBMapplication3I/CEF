// <autogenerated>
// <copyright file="Mapping.Stores.StoreContact.cs" company="clarity-ventures.com">
// Copyright (c) 2016-2023 clarity-ventures.com. All rights reserved.
// </copyright>
// <summary>Implements the Stores section of the Mapping class</summary>
// <remarks>This file was auto-generated by Mapping.tt, changes to this
// file will be overwritten automatically when the T4 template is run again</remarks>
// </autogenerated>
// ReSharper disable CyclomaticComplexity, FunctionComplexityOverflow, InvokeAsExtensionMethod, MergeCastWithTypeCheck
// ReSharper disable MissingLinebreak, RedundantDelegateInvoke, RedundantUsingDirective
#pragma warning disable CS0618 // Ignore Obsolete warnings
#nullable enable
namespace Clarity.Ecommerce.Mapper
{
    using System;
    using System.Collections.Generic;
    using System.Data.Entity;
    using System.Linq;
    using System.Linq.Expressions;
    using DataModel;
    using Interfaces.DataModel;
    using Interfaces.Models;
    using LinqKit;
    using MoreLinq;
    using Utilities;

    public static partial class ModelMapperForStoreContact
    {
        public sealed class AnonStoreContact : StoreContact
        {
            // public new Store? Master { get; set; }
        }

        public static readonly Func<StoreContact?, string?, IStoreContactModel?> MapStoreContactModelFromEntityFull = CreateStoreContactModelFromEntityFull;

        public static readonly Func<StoreContact?, string?, IStoreContactModel?> MapStoreContactModelFromEntityLite = CreateStoreContactModelFromEntityLite;

        public static readonly Func<StoreContact?, string?, IStoreContactModel?> MapStoreContactModelFromEntityList = CreateStoreContactModelFromEntityList;

        public static Func<IStoreContact, IStoreContactModel, string?, IStoreContactModel>? CreateStoreContactModelFromEntityHooksFull { get; set; }

        public static Func<IStoreContact, IStoreContactModel, string?, IStoreContactModel>? CreateStoreContactModelFromEntityHooksLite { get; set; }

        public static Func<IStoreContact, IStoreContactModel, string?, IStoreContactModel>? CreateStoreContactModelFromEntityHooksList { get; set; }

        public static Expression<Func<StoreContact, AnonStoreContact>>? PreBuiltStoreContactSQLSelectorFull { get; set; }

        public static Expression<Func<StoreContact, AnonStoreContact>>? PreBuiltStoreContactSQLSelectorLite { get; set; }

        public static Expression<Func<StoreContact, AnonStoreContact>>? PreBuiltStoreContactSQLSelectorList { get; set; }

        /// <summary>An <see cref="IStoreContactModel"/> extension method that creates a(n) <see cref="StoreContact"/> entity.</summary>
        /// <param name="model">            The model to act on.</param>
        /// <param name="timestamp">        The timestamp Date/Time.</param>
        /// <param name="contextProfileName">Name of the context profile.</param>
        /// <returns>The new <see cref="StoreContact"/> entity.</returns>
        public static IStoreContact CreateStoreContactEntity(
            this IStoreContactModel model,
            DateTime timestamp,
            string? contextProfileName)
        {
            // Create the object and Map the Inherited Properties
            return Contract.RequiresNotNull(model)
                .CreateEntityFromModelNameableBase<IStoreContactModel, StoreContact>(timestamp, contextProfileName)
                // Use the Update method to map non-inherited properties
                .UpdateStoreContactFromModel(model, timestamp, null);
        }

        /// <summary>An <see cref="IStoreContactModel"/> extension method that updates a(n) <see cref="StoreContact"/> entity.</summary>
        /// <param name="entity">         The entity to act on.</param>
        /// <param name="model">          The model to read from.</param>
        /// <param name="timestamp">      The timestamp Date/Time.</param>
        /// <param name="updateTimestamp">The update timestamp Date/Time.</param>
        /// <returns>The updated <see cref="StoreContact"/> entity.</returns>
        public static IStoreContact UpdateStoreContactFromModel(
            this IStoreContact entity,
            IStoreContactModel model,
            DateTime timestamp,
            DateTime? updateTimestamp)
        {
            // Map the Inherited Properties
            entity = Contract.RequiresNotNull(entity)
                .MapNameableBaseModelPropertiesToEntity(Contract.RequiresNotNull(model));
            // StoreContact's Related Objects
            // Finally, update the timestamp
            entity.UpdatedDate = updateTimestamp == null || updateTimestamp == DateTime.MinValue ? null : updateTimestamp;
            // Return
            return entity;
        }

        public static void GenStoreContactSQLSelectorFull()
        {
            PreBuiltStoreContactSQLSelectorFull = x => x == null ? null! : new AnonStoreContact
            {
                MasterID = x.MasterID,
                Master = ModelMapperForStore.PreBuiltStoreSQLSelectorList.Expand().Compile().Invoke(x.Master!),
                SlaveID = x.SlaveID,
                Slave = ModelMapperForContact.PreBuiltContactSQLSelectorList.Expand().Compile().Invoke(x.Slave!),
                Name = x.Name,
                Description = x.Description,
                ID = x.ID,
                CustomKey = x.CustomKey,
                CreatedDate = x.CreatedDate,
                UpdatedDate = x.UpdatedDate,
                Active = x.Active,
                Hash = x.Hash,
                JsonAttributes = x.JsonAttributes,
            };
        }

        public static void GenStoreContactSQLSelectorLite()
        {
            PreBuiltStoreContactSQLSelectorLite = x => x == null ? null! : new AnonStoreContact
            {
                MasterID = x.MasterID,
                Master = ModelMapperForStore.PreBuiltStoreSQLSelectorList.Expand().Compile().Invoke(x.Master!),
                SlaveID = x.SlaveID,
                Slave = ModelMapperForContact.PreBuiltContactSQLSelectorList.Expand().Compile().Invoke(x.Slave!),
                Name = x.Name,
                Description = x.Description,
                ID = x.ID,
                CustomKey = x.CustomKey,
                CreatedDate = x.CreatedDate,
                UpdatedDate = x.UpdatedDate,
                Active = x.Active,
                Hash = x.Hash,
                JsonAttributes = x.JsonAttributes,
            };
        }

        public static void GenStoreContactSQLSelectorList()
        {
            PreBuiltStoreContactSQLSelectorList = x => x == null ? null! : new AnonStoreContact
            {
                MasterID = x.MasterID,
                Master = ModelMapperForStore.PreBuiltStoreSQLSelectorList.Expand().Compile().Invoke(x.Master!), // For Flattening Properties (List)
                SlaveID = x.SlaveID,
                Slave = ModelMapperForContact.PreBuiltContactSQLSelectorList.Expand().Compile().Invoke(x.Slave!), // For Flattening Properties (List)
                Name = x.Name,
                ID = x.ID,
                CustomKey = x.CustomKey,
                CreatedDate = x.CreatedDate,
                Active = x.Active,
                Hash = x.Hash,
                JsonAttributes = x.JsonAttributes,
            };
        }

        public static IEnumerable<IStoreContactModel> SelectFullStoreContactAndMapToStoreContactModel(
            this IQueryable<StoreContact> dbSetWithFilters,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltStoreContactSQLSelectorFull == null);
            return dbSetWithFilters
                .AsExpandable()
                .Select(PreBuiltStoreContactSQLSelectorFull!.Compile())
                .ToList()
                .Select(x => CreateStoreContactModelFromEntityFull(x, contextProfileName))!;
        }

        public static IEnumerable<IStoreContactModel> SelectLiteStoreContactAndMapToStoreContactModel(
            this IQueryable<StoreContact> dbSetWithFilters,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltStoreContactSQLSelectorLite == null);
            return dbSetWithFilters
                .AsExpandable()
                .Select(PreBuiltStoreContactSQLSelectorLite!.Compile())
                .ToList()
                .Select(x => CreateStoreContactModelFromEntityLite(x, contextProfileName))!;
        }

        public static IEnumerable<IStoreContactModel> SelectListStoreContactAndMapToStoreContactModel(
            this IQueryable<StoreContact> dbSetWithFilters,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltStoreContactSQLSelectorList == null);
            return dbSetWithFilters
                .AsExpandable()
                .Select(PreBuiltStoreContactSQLSelectorList!.Compile())
                .ToList()
                .Select(x => CreateStoreContactModelFromEntityList(x, contextProfileName))!;
        }

        public static IStoreContactModel? SelectFirstFullStoreContactAndMapToStoreContactModel(
            this IQueryable<StoreContact> dbSetWithFilters,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltStoreContactSQLSelectorFull == null);
            return dbSetWithFilters
                .AsExpandable()
                .OrderBy(x => x.ID)
                .Take(1)
                .Select(PreBuiltStoreContactSQLSelectorFull!.Compile())
                .ToList()
                .Select(x => CreateStoreContactModelFromEntityFull(x, contextProfileName))
                .FirstOrDefault();
        }

        public static IStoreContactModel? SelectFirstListStoreContactAndMapToStoreContactModel(
            this IQueryable<StoreContact> dbSetWithFilters,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltStoreContactSQLSelectorList == null);
            return dbSetWithFilters
                .AsExpandable()
                .OrderBy(x => x.ID)
                .Take(1)
                .Select(PreBuiltStoreContactSQLSelectorList!.Compile())
                .ToList()
                .Select(x => CreateStoreContactModelFromEntityList(x, contextProfileName))
                .FirstOrDefault();
        }

        public static IStoreContactModel? SelectSingleFullStoreContactAndMapToStoreContactModel(
            this IQueryable<StoreContact> dbSetWithFilters,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltStoreContactSQLSelectorFull == null);
            return dbSetWithFilters
                .AsExpandable()
                .OrderBy(x => x.ID)
                .Take(1)
                .Select(PreBuiltStoreContactSQLSelectorFull!.Compile())
                .ToList()
                .Select(x => CreateStoreContactModelFromEntityFull(x, contextProfileName))
                .SingleOrDefault();
        }

        public static IStoreContactModel? SelectSingleLiteStoreContactAndMapToStoreContactModel(
            this IQueryable<StoreContact> dbSetWithFilters,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltStoreContactSQLSelectorLite == null);
            return dbSetWithFilters
                .AsExpandable()
                .OrderBy(x => x.ID)
                .Take(1)
                .Select(PreBuiltStoreContactSQLSelectorLite!.Compile())
                .ToList()
                .Select(x => CreateStoreContactModelFromEntityLite(x, contextProfileName))
                .SingleOrDefault();
        }

        public static IStoreContactModel? SelectSingleListStoreContactAndMapToStoreContactModel(
            this IQueryable<StoreContact> dbSetWithFilters,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltStoreContactSQLSelectorList == null);
            return dbSetWithFilters
                .AsExpandable()
                .OrderBy(x => x.ID)
                .Take(1)
                .Select(PreBuiltStoreContactSQLSelectorList!.Compile())
                .ToList()
                .Select(x => CreateStoreContactModelFromEntityList(x, contextProfileName))
                .SingleOrDefault();
        }

        public static (IEnumerable<IStoreContactModel> results, int totalPages, int totalCount) SelectFullStoreContactAndMapToStoreContactModel(
            this IQueryable<StoreContact> dbSetWithFilters,
            Paging? paging,
            Sort[]? sorts,
            Grouping[]? groupings,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltStoreContactSQLSelectorFull == null);
            return (dbSetWithFilters
                    .AsExpandable()
                    .ApplySorting(sorts, groupings, contextProfileName)
                    .FilterByPaging(paging, out var totalPages, out var totalCount)
                    .Select(PreBuiltStoreContactSQLSelectorFull!.Compile())
                    .ToList()
                    .Select(x => CreateStoreContactModelFromEntityFull(x, contextProfileName)),
                totalPages,
                totalCount)!;
        }

        public static (IEnumerable<IStoreContactModel> results, int totalPages, int totalCount) SelectLiteStoreContactAndMapToStoreContactModel(
            this IQueryable<StoreContact> dbSetWithFilters,
            Paging? paging,
            Sort[]? sorts,
            Grouping[]? groupings,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltStoreContactSQLSelectorLite == null);
            return (dbSetWithFilters
                    .AsExpandable()
                    .ApplySorting(sorts, groupings, contextProfileName)
                    .FilterByPaging(paging, out var totalPages, out var totalCount)
                    .Select(PreBuiltStoreContactSQLSelectorLite!.Compile())
                    .ToList()
                    .Select(x => CreateStoreContactModelFromEntityLite(x, contextProfileName)),
                totalPages,
                totalCount)!;
        }

        public static (IEnumerable<IStoreContactModel> results, int totalPages, int totalCount) SelectListStoreContactAndMapToStoreContactModel(
            this IQueryable<StoreContact> dbSetWithFilters,
            Paging? paging,
            Sort[]? sorts,
            Grouping[]? groupings,
            string? contextProfileName)
        {
            BaseModelMapper.Initialize(PreBuiltStoreContactSQLSelectorList == null);
            return (dbSetWithFilters
                    .AsExpandable()
                    .ApplySorting(sorts, groupings, contextProfileName)
                    .FilterByPaging(paging, out var totalPages, out var totalCount)
                    .Select(PreBuiltStoreContactSQLSelectorList!.Compile())
                    .ToList()
                    .Select(x => CreateStoreContactModelFromEntityList(x, contextProfileName)),
                totalPages,
                totalCount)!;
        }

        public static IStoreContactModel? CreateStoreContactModelFromEntityFull(this IStoreContact? entity, string? contextProfileName)
        {
            return CreateStoreContactModelFromEntity(entity, MappingMode.Full, contextProfileName);
        }

        public static IStoreContactModel? CreateStoreContactModelFromEntityLite(this IStoreContact? entity, string? contextProfileName)
        {
            return CreateStoreContactModelFromEntity(entity, MappingMode.Lite, contextProfileName);
        }

        public static IStoreContactModel? CreateStoreContactModelFromEntityList(this IStoreContact? entity, string? contextProfileName)
        {
            return CreateStoreContactModelFromEntity(entity, MappingMode.List, contextProfileName);
        }

        public static IStoreContactModel? CreateStoreContactModelFromEntity(
            this IStoreContact? entity,
            MappingMode mode,
            string? contextProfileName)
        {
            if (entity == null) { return null; }
            // Map the Inherited Properties
            // ReSharper disable once InvokeAsExtensionMethod
            var model = BaseModelMapper.MapNameableBaseEntityPropertiesToModel(
                RegistryLoaderWrapper.GetInstance<IStoreContactModel>(contextProfileName),
                Contract.RequiresNotNull(entity),
                mode,
                contextProfileName);
            // Map this level's Properties
            switch (mode)
            {
                case MappingMode.Full: //////////////////////////////////////////////////////////////////////////////////////////////////////////////
                {
                    // StoreContact's Properties
                    // StoreContact's Related Objects
                    // StoreContact's Associated Objects
                    // Additional Mappings
                    if (CreateStoreContactModelFromEntityHooksFull != null) { model = CreateStoreContactModelFromEntityHooksFull(entity, model, contextProfileName); }
                    goto case MappingMode.Lite;
                }
                case MappingMode.Lite: //////////////////////////////////////////////////////////////////////////////////////////////////////////////
                {
                    // StoreContact's Properties
                    // StoreContact's Related Objects (Not Mapped unless Forced, or a flattening property)
                    model.Slave = ModelMapperForContact.CreateContactModelFromEntityLite(entity.Slave, contextProfileName);
                    // StoreContact's Associated Objects (Not Mapped unless Forced)
                    // Additional Mappings
                    if (CreateStoreContactModelFromEntityHooksLite != null) { model = CreateStoreContactModelFromEntityHooksLite(entity, model, contextProfileName); }
                    goto case MappingMode.List;
                }
                case MappingMode.List: //////////////////////////////////////////////////////////////////////////////////////////////////////////////
                default:
                {
                    // IHaveJsonAttributesBase Properties (Forced)
                    model.SerializableAttributes = entity.JsonAttributes.DeserializeAttributesDictionary();
                    // StoreContact's Properties
                    // StoreContact's Related Objects (Not Mapped unless Forced, or a flattening property)
                    model.MasterID = entity.MasterID;
                    model.MasterKey = entity.Master?.CustomKey;
                    model.MasterName = entity.Master?.Name;
                    model.SlaveID = entity.SlaveID;
                    model.SlaveKey = entity.Slave?.CustomKey;
                    // StoreContact's Associated Objects (Not Mapped unless Forced)
                    // Additional Mappings
                    if (CreateStoreContactModelFromEntityHooksList != null) { model = CreateStoreContactModelFromEntityHooksList(entity, model, contextProfileName); }
                    break;
                }
            } ///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
            // Finished!
            return model;
        }
    }
}
